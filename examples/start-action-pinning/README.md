# Start Action and Pinning

Registers one stable V3 Start action. It remains in MOD MENUS when pinned and
uses `owner_id + entry_id` identity. Select or the explicit pin icon controls
the per-save pin; the source mod still owns its callback.

Build locally with `.\build_release.ps1`.
