# Extra Summary Page

Adds a purpose-named TRAINING page to the production Summary presenter. The
page uses the existing L envelope and therefore does not resize Summary when
the player changes pages or Pokémon.

Build locally with `.\build_release.ps1`.
