# Dropdown Screen

Adds a Mod Menus action that opens a controlled dropdown. The example includes
groups, a disabled option, descriptions, and enough choices to exercise
scrolling and edge placement.

Build locally with `.\build_release.ps1`.
