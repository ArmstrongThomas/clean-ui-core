# Animated Shader Grid

Demonstrates the custom-surface escape hatch: a 5×4 logical grid, per-frame
animation, pointer regions, and a scoped shader rendered on a private canvas.
The host restores all graphics state after every callback.

Build locally with `.\build_release.ps1`.
