# Modal Overlay

Adds a Mod Menus action that returns a dimmed modal overlay. Cancel and outside
dismissal do not mutate state, and closing restores focus to the trigger.

Build locally with `.\build_release.ps1`.
