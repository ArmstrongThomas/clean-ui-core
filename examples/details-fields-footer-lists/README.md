# Details Fields and Footer Lists

Opens a details card with automatic field columns, bottom-anchored lists, and
sprite-space reservation. No manual bottom-up coordinates are needed.

Build locally with `.\build_release.ps1`.
