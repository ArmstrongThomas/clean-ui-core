# Party Row Colors

Shows declarative `party.row_style` rules for alternating slots, status,
and Egg rows. There is no draw callback and no source-state mutation.

Build locally with `.\build_release.ps1`. The deterministic ZIP is written
to this example's `dist` directory unless `-OutputDirectory` is supplied.
